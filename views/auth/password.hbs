<div class="row">
    <div class="col s6 offset-s3">
        <!--  выводим ошибку, если есть    -->
        {{#if error}}
            <p class="alert">{{error}}</p>
        {{/if}}

        <h3>Задать новый пароль</h3>
        <!-- Login -->
        <form action="/auth/password" method="post">
            <!--    user_name (email)    -->
            <div class="input-field">
                <input id="password" name="password" type="password" class="validate" required>
                <label for="password">Новый пароль</label>
                <span class="helper-text" data-error="Введите пароль" data-success="OK"></span>
            </div>

            <!--  req.csrfToken() вставит в value `token,
            который будет назначен res.locals.csrf в  variables.js  -->
            <input type="hidden" name="_csrf" value="{{csrf}}">
            <input type="hidden" name="userId" value="{{userId}}">
            <input type="hidden" name="token" value="{{token}}">

            <button class="btn btn-primary" type="submit">Обновить пароль</button>

        </form>

    </div>
</div>